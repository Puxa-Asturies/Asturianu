on:
  push:
    branches: [ main ]
jobs:
  build:
    runs-on: ubuntu-latest
    container:
      image: librelingo/librelingo:latest
    steps:
      - uses: actions/checkout@v4
      - name: Build Course Webapp
        run: librelingo build
